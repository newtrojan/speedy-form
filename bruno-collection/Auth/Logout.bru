meta {
  name: Logout
  type: http
  seq: 4
}

post {
  url: {{base_url}}/api/v1/auth/logout/
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

docs {
  Refresh token is read from httpOnly cookie automatically.
  No request body needed - backend reads from cookies and blacklists the token.
}
